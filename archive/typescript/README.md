# Archived TypeScript Backend and CLI

This directory keeps the superseded TypeScript backend, CLI, migration helper, and parity harness for rollback/reference while the active implementation lives in Go.

Active code:

```text
cmd/api
cmd/agentbox
cmd/migrate
internal/agentbox
app
```

The Next.js dashboard remains active under `app/`. Do not use the files in this archive for release builds.
